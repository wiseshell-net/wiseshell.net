services:
  zola:
    build:
      context: .
      args:
        ZOLA_VERSION: "0.23.5"
    platform: ${PLATFORM:-linux/amd64}
    working_dir: /site
    volumes:
      - .:/site
    ports:
      - "1111:1111"
    command: ["zola", "serve", "--interface", "0.0.0.0", "--port", "1111"]
